---
const { title, src, href, about } = Astro.props;
---
<a href={href} class="card">
    <img src={src} alt="card">
    <div class="content">
        <div class="title">{title}</div>
        <p>{about}</p>
    </div>
</a>

<style>
    .card{
        display:grid;
        grid-template-rows: repeat(2, 1fr);
        background-color: var(--accent);
        border-radius: 0.5rem;
        box-shadow: 0 0 8px var(--accent);
        aspect-ratio: 3 / 4;
        overflow: hidden;
        color: var(--base);
        position: relative;
        transition: box-shadow .2s;
    }
    .card:hover{
        box-shadow: 0 0 12px var(--accent);
    }
    .card:hover img{
        transform: scale(1.1);
    }
    .card:hover .content{
        box-shadow: 0 0 30px var(--base);
    }
    .card img{
        width: 100%;
        transition: transform .4s ease-out;
    }
    .card .title{
        padding: 0.2rem;
        font-size: 1.2rem;
        font-weight: bold;
    }
    .card p{
        margin-top: 0.5rem;
        font-size: 1rem;
    }
    .card .content{
        position: absolute;
        top: 50%;
        width: 100%;
        height: 50%;
        box-sizing: border-box;
        padding: 1rem;
        background: var(--accent);
        transition: box-shadow .2s;
    }
</style>